---
layout: bare
title: "Perry Soil Map"
permalink: /perry-soil-map/
---

<link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css">
<style>
:root{--panel-bg:rgba(255,255,255,.96);--panel-border:#dfe3e8;--text:#1d1d1f;--muted:#4b5563;--accent:#2d6a4f;--accent-2:#6cae75;--shadow:0 10px 22px rgba(17,24,39,.1)}
.main-content,main#content{max-width:1500px!important;width:100%!important;box-sizing:border-box}
#mapWrap{display:grid;grid-template-columns:250px minmax(0,1fr) 320px;gap:18px;align-items:start;margin:1.2rem 0 2rem}
#legendColumn,#mapColumn,#infoPanel{min-width:0}.panel,#legend,#layerControls,#infoPanel{background:var(--panel-bg);border:1px solid var(--panel-border);border-radius:12px;box-shadow:var(--shadow)}
#legend,#infoPanel{position:sticky;top:12px}#legend{padding:14px 14px 10px}#infoPanel{padding:16px;min-height:240px}
#legend h4,#infoPanel h3{margin:0 0 8px;color:var(--text)}#legendContent{display:flex;flex-direction:column;gap:12px;min-height:80px}.legend-section{border-top:1px solid #e5e7eb;padding-top:10px;animation:legendPop .22s ease-out both}.legend-section:first-child{border-top:0;padding-top:0}.legend-section h5{font-size:.78rem;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);margin:0 0 8px}.legend-item{display:flex;align-items:center;gap:8px;font-size:.92rem;color:var(--text);margin:5px 0;padding:4px 6px;border-radius:8px;cursor:pointer}.legend-item:hover{background:rgba(45,106,79,.05)}.legend-item.active{background:rgba(45,106,79,.07)}.legend-item.dimmed{opacity:.45}.legend-item.static{cursor:default}.legend-swatch{width:18px;height:18px;border-radius:4px;border:1px solid rgba(0,0,0,.25);display:inline-block;flex:0 0 18px}.legend-swatch.circle{border-radius:50%;width:12px;height:12px;flex-basis:12px}.legend-swatch.line{width:28px;height:3px;border:0}.legend-note{margin:10px 0 0;font-size:.78rem;color:var(--muted);line-height:1.4}.ramp-row{display:flex;align-items:center;gap:8px;font-size:.82rem;line-height:1.5;margin:2px 0}.ramp-chip{width:20px;height:13px;flex:0 0 20px;border:1px solid rgba(0,0,0,.25)}
#map{height:560px;width:100%;border-radius:12px;border:1px solid var(--panel-border);box-shadow:var(--shadow);background:#d8e8f3}#layerControls{margin-top:12px;padding:10px 12px}.control-row{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:8px 0;border-bottom:1px solid #edf2f7}.control-row:last-child{border-bottom:0}.control-row label{display:flex;align-items:center;gap:8px;flex:1;font-size:.92rem;color:var(--text)}.control-row input[type=checkbox]{accent-color:var(--accent)}.control-row input[type=range]{width:120px;accent-color:var(--accent-2)}#infoPanel .muted,.pedon-coords{color:var(--muted)}.pedon-id{margin:0 0 2px;font-weight:700;font-size:1rem}.pedon-series{margin:0 0 2px;font-size:1.02rem}.pedon-coords{margin:0 0 12px;font-size:.8rem;font-variant-numeric:tabular-nums}
.image-carousel{position:relative;border-radius:10px;overflow:hidden;background:#f1f5f9;border:1px solid var(--panel-border)}.carousel-frame{height:260px;display:flex;align-items:center;justify-content:center}.carousel-image{display:none;max-width:100%;max-height:260px;object-fit:contain;cursor:zoom-in}.carousel-image.active{display:block}.carousel-image.missing{display:none}.carousel-arrow,.modal-arrow{position:absolute;top:50%;transform:translateY(-50%);border:0;background:rgba(15,23,42,.5);color:#fff;width:30px;height:44px;font-size:1.4rem;cursor:pointer;border-radius:6px}.carousel-arrow.prev,.modal-arrow.prev{left:6px}.carousel-arrow.next,.modal-arrow.next{right:6px}.carousel-caption{text-align:center;font-size:.8rem;color:var(--muted);padding:6px 0 2px;background:#fff}.carousel-dots{display:flex;justify-content:center;gap:6px;padding:4px 0 8px;background:#fff}.dot{width:8px;height:8px;border-radius:50%;background:#cbd5e1;cursor:pointer}.dot.active{background:var(--accent)}
.popup-img{display:block;margin-top:6px;max-width:275px;max-height:250px;width:auto;border-radius:6px;cursor:pointer}.leaflet-popup-content{font-size:.95rem}.modal{position:fixed;inset:0;background:rgba(15,23,42,.72);display:none;align-items:center;justify-content:center;z-index:2000;padding:24px}.modal.open{display:flex}.modal-card{position:relative;max-width:min(90vw,900px);background:#fff;border-radius:14px;padding:16px 16px 12px;box-shadow:0 18px 42px rgba(15,23,42,.35)}.modal-close{position:absolute;top:8px;right:10px;border:0;background:rgba(15,23,42,.08);border-radius:50%;width:32px;height:32px;cursor:pointer;font-size:1.25rem}.modal-carousel{position:relative}.modal-carousel-image{display:none;max-width:min(88vw,840px);max-height:72vh;object-fit:contain;border-radius:10px}.modal-carousel-image.active{display:block}.modal-caption{text-align:center;margin:10px 0 0;color:var(--text)}.close-note{text-align:center;margin-top:8px;font-size:.8rem;color:var(--muted)}
@keyframes legendPop{from{opacity:0;transform:translateY(-6px) scale(.98)}to{opacity:1;transform:none}}@media(prefers-reduced-motion:reduce){.legend-section{animation:none}}@media(max-width:1240px){#mapWrap{grid-template-columns:230px minmax(0,1fr)}#infoPanel{grid-column:1/-1;position:static}}@media(max-width:860px){#mapWrap{grid-template-columns:1fr}#legend{position:static}}
</style>

<h1>Perry Soil Map</h1>
<p>This interactive map shows pedon observations at UGA GrandFarm in Perry, Georgia, together with ordinary-kriged predictions of soil chemistry and texture. Use the layer controls to compare surface soil measurements and the mapped soil series.</p>
<div id="mapWrap"><aside id="legendColumn"><div id="legend"><h4>Legend</h4><div id="legendContent"></div><p id="unitNote" class="legend-note"></p></div></aside><div id="mapColumn"><div id="map"></div><div id="layerControls" aria-label="Map layer controls"></div></div><aside id="infoPanel" aria-live="polite"><h3>Pedon Points</h3><div id="infoContent"><p class="muted">Click a pedon point to view its soil profile and field photo.</p></div></aside></div>
<div id="imgModal" class="modal" role="dialog" aria-modal="true" onclick="closeModal(event)"><div class="modal-card" onclick="event.stopPropagation()"><button class="modal-close" type="button" aria-label="Close gallery" onclick="closeModal()">×</button><div id="modalCarousel" class="modal-carousel"></div><button class="modal-arrow prev" onclick="changeModalImage(-1)" aria-label="Previous photo">&#8249;</button><button class="modal-arrow next" onclick="changeModalImage(1)" aria-label="Next photo">&#8250;</button><div id="modalDots" class="carousel-dots"></div><div id="modalCaption" class="modal-caption"></div><div class="close-note">Click outside or press ESC to close</div></div></div>
<script src="https://unpkg.com/leaflet/dist/leaflet.js"></script><script src="https://cdn.jsdelivr.net/npm/papaparse@5.4.1/papaparse.min.js"></script><script>
const base='{{ "/assets/" | relative_url }}',imgBase='{{ "/assets/images/pedon_images/" | relative_url }}',fieldPhotoBase='https://github.com/madelynwillis3/research/releases/download/coastalplain-images-v1.0/perry_GA_point_',excluded=new Set(['39','50','51','58','59','60','65','74']);
const colors={Faceville:'#d73027',Orangeburg:'#c94c4c',Lucy:'#e76f51',Troup:'#f4a6a6',Greenville:'#8b0000',Leefield:'#d8c3a5',Blanton:'#8a7f73',Norfolk:'#f28c28',Dothan:'#d4a017',Johns:'#c2a878','Red Bay':'#5c0000',Benevolence:'#fa8072',Lakeland:'#d2a679',Wagram:'#d2a679',Bonneau:'#d3d3d3',Esto:'#c9a44b',Marvyn:'#a44a3f',Disturbed:'#808080'};
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const links=Object.fromEntries(Object.keys(colors).filter(s=>s!=='Disturbed').map(s=>[s,`https://casoilresource.lawr.ucdavis.edu/sde/?series=${encodeURIComponent(s.toUpperCase())}#osd`]));
const seriesHTML=s=>links[s]?`<a target="_blank" rel="noopener noreferrer" href="${links[s]}">${esc(s)}</a>`:esc(s);
const map=L.map('map').setView([32.43,-83.73],14);map.createPane('soilRaster').style.zIndex=200;map.createPane('soilVector').style.zIndex=400;map.createPane('pedons').style.zIndex=650;map.getPane('pedons').style.pointerEvents='auto';L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{attribution:'Tiles © Esri'}).addTo(map);
const rasters={},pedons=L.layerGroup().addTo(map),soil=L.geoJSON(null,{pane:'soilVector',style:f=>({color:'#333',weight:1,fillColor:f.properties?.fill||'#bbb',fillOpacity:.72})}),match=L.geoJSON(null,{pane:'soilVector',style:f=>({color:'#666',weight:.2,fillColor:f.properties?.fill||'#d8d8d8',fillOpacity:.68})}),units=L.geoJSON(null,{pane:'soilVector',style:()=>({color:'#4d4d4d',weight:1.5,opacity:.9,fillOpacity:0,dashArray:'6,4'})}).addTo(map),border=L.geoJSON(null,{pane:'soilVector',style:()=>({color:'#1f2937',weight:2.5,opacity:.9,fillOpacity:0})}).addTo(map);
const state={manifest:null,activeRaster:null,activeSeries:null,rasterOpacity:.8,soil:false,match:false,boundary:true,pedons:true,seriesCounts:{},matchCounts:{}};let entries=[],currentEntry=null,panelIndex=0,modalIndex=0;const captions=['Soil profile','Field photo'];
function renderLegend(){let s=[];const r=state.manifest?.rasters?.find(x=>x.id===state.activeRaster);if(r){let rows=[];for(let i=r.colors.length-1;i>=0;i--)rows.push(`<div class="ramp-row"><span class="ramp-chip" style="background:${r.colors[i]}"></span><span>${r.breaks[i]} – ${r.breaks[i+1]}</span></div>`);s.push(`<section class="legend-section"><h5>${esc(r.title)}${r.unit?' ('+esc(r.unit)+')':''}</h5>${rows.join('')}</section>`)}if(state.soil&&Object.keys(state.seriesCounts).length){const p=state.manifest.vectors?.soil_series?.palette||{};s.push(`<section class="legend-section"><h5>Mapped soil series</h5>${Object.entries(state.seriesCounts).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="legend-item static"><i class="legend-swatch" style="background:${p[k]||'#bbb'}"></i>${esc(k)} (${v})</div>`).join('')}</section>`)}if(state.match&&Object.keys(state.matchCounts).length){const p=state.manifest.vectors?.ssurgo_matchup?.palette||{};s.push(`<section class="legend-section"><h5>SSURGO agreement</h5>${['Agree','Disagree'].filter(k=>state.matchCounts[k]!=null).map(k=>`<div class="legend-item static"><i class="legend-swatch" style="background:${p[k]||'#999'}"></i>${k} (${state.matchCounts[k]})</div>`).join('')}</section>`)}s.push(`<section class="legend-section"><h5>Boundaries</h5>${state.boundary?'<div class="legend-item static"><i class="legend-swatch line" style="background:repeating-linear-gradient(90deg,#4d4d4d 0 6px,transparent 6px 10px)"></i>SSURGO map unit</div>':''}<div class="legend-item static"><i class="legend-swatch line" style="background:#1f2937"></i>Site border</div></section>`);if(state.pedons){const c={};entries.forEach(e=>c[e.series]=(c[e.series]||0)+1);s.push(`<section class="legend-section"><h5>Pedon points (${entries.length})</h5>${Object.entries(c).sort((a,b)=>b[1]-a[1]).map(([k,v])=>`<div class="legend-item ${state.activeSeries===k?'active':''} ${state.activeSeries&&state.activeSeries!==k?'dimmed':''}" onclick="selectSeries('${k.replace(/'/g,"\\'")}')"><i class="legend-swatch circle" style="background:${colors[k]||'#777'}"></i>${seriesHTML(k)} (${v})</div>`).join('')}${state.activeSeries?'<div class="legend-item" onclick="selectSeries(null)">↶ Show all series</div>':''}</section>`)}document.getElementById('legendContent').innerHTML=s.join('');const unit=r?.unit==='lbs/ac'?state.manifest?.unit_note||'':'';document.getElementById('unitNote').textContent=unit}
function selectSeries(s){state.activeSeries=state.activeSeries===s?null:s;pedons.eachLayer(m=>m.setStyle(markerStyle(m._entry)));renderLegend()}
function markerStyle(e){const fill=colors[e.series]||'#808080',base={pane:'pedons',color:'#000',fillColor:fill,weight:1.5,fillOpacity:.9,opacity:1,radius:6};if(state.activeSeries&&e.series!==state.activeSeries)return {...base,fillOpacity:.15,opacity:.25,radius:5};if(state.activeSeries)return {...base,radius:8,weight:2};return base}
function carousel(images,label){return `<div class="image-carousel"><div class="carousel-frame">${images.map((src,i)=>`<img class="carousel-image ${i?'':'active'}" src="${src}" alt="${esc(label)} ${captions[i]||'photo'}" onclick="openModal(${i})" onerror="this.classList.add('missing')">`).join('')}</div>${images.length>1?'<button class="carousel-arrow prev" onclick="changePanelImage(-1)">‹</button><button class="carousel-arrow next" onclick="changePanelImage(1)">›</button>':''}<div class="carousel-caption" id="panelCaption">${captions[0]}</div>${images.length>1?`<div class="carousel-dots">${images.map((_,i)=>`<i class="dot ${i?'':'active'}" onclick="showPanelImage(${i})"></i>`).join('')}</div>`:''}</div>`}
function showPanelImage(i){const imgs=document.querySelectorAll('#infoContent .carousel-image'),dots=document.querySelectorAll('#infoContent .dot');if(!imgs.length)return;panelIndex=(i+imgs.length)%imgs.length;imgs.forEach((x,k)=>x.classList.toggle('active',k===panelIndex));dots.forEach((x,k)=>x.classList.toggle('active',k===panelIndex));document.getElementById('panelCaption').textContent=captions[panelIndex]||`Photo ${panelIndex+1}`}function changePanelImage(d){showPanelImage(panelIndex+d)}
function setInfoPanel(e){currentEntry=e;panelIndex=0;document.getElementById('infoContent').innerHTML=e?`<p class="pedon-id">Point ${esc(e.label)}</p><p class="pedon-series">${seriesHTML(e.series)}</p><p class="pedon-coords">${e.lat.toFixed(5)}, ${e.lng.toFixed(5)}</p>${carousel(e.images,e.label)}`:'<p class="muted">Click a pedon point to view its soil profile and field photo.</p>'}
function openModal(i=0){if(!currentEntry)return;modalIndex=i;document.getElementById('modalCarousel').innerHTML=currentEntry.images.map((src,k)=>`<img class="modal-carousel-image ${k===i?'active':''}" src="${src}" alt="${esc(currentEntry.label)} ${captions[k]||'photo'}">`).join('');document.getElementById('modalDots').innerHTML=currentEntry.images.length>1?currentEntry.images.map((_,k)=>`<i class="dot ${k===i?'active':''}" onclick="goModal(${k})"></i>`).join(''):'';document.getElementById('modalCaption').textContent=`Point ${currentEntry.label} — ${captions[i]||'photo'}`;document.querySelectorAll('.modal-arrow').forEach(x=>x.style.display=currentEntry.images.length>1?'':'none');document.getElementById('imgModal').classList.add('open')}function goModal(i){modalIndex=i;openModal(i)}function changeModalImage(d){if(!currentEntry)return;modalIndex=(modalIndex+d+currentEntry.images.length)%currentEntry.images.length;openModal(modalIndex)}function closeModal(e){if(e&&e.target!==e.currentTarget&&e.target!==document.getElementById('imgModal'))return;document.getElementById('imgModal').classList.remove('open')}document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal();if(document.getElementById('imgModal').classList.contains('open')){if(e.key==='ArrowLeft')changeModalImage(-1);if(e.key==='ArrowRight')changeModalImage(1)}});
function addMarkers(data){pedons.clearLayers();entries=[];data.forEach(row=>{const id=String(row['Point ID']||row.PointID||row.Point||row.label||'').trim();if(!id||excluded.has(id))return;const lat=Number(row.y??row.Latitude??row.latitude??row.lat),lng=Number(row.x??row.Longitude??row.longitude??row.lng);if(!Number.isFinite(lat)||!Number.isFinite(lng))return;const e={label:id,lat,lng,series:row.Series||row.series||'Unknown',images:[`${imgBase}${id}.jpg`,`${fieldPhotoBase}${id}.jpg`]};entries.push(e);const m=L.circleMarker([lat,lng],markerStyle(e));m._entry=e;m.bindPopup(`<b>${seriesHTML(e.series)}</b><br>Point ${esc(id)}<br><img src="${e.images[0]}" class="popup-img" alt="Profile image" onclick="setInfoPanel(window.__pedons['${esc(id)}']);openModal(0)" onerror="this.style.display='none'">`,{maxWidth:325});m.on('click',()=>setInfoPanel(e)).addTo(pedons);window.__pedons=window.__pedons||{};window.__pedons[id]=e});setInfoPanel(null);renderLegend()}
async function loadMap(){state.manifest=await fetch(base+'maplayers/manifest.json').then(r=>r.json());const controls=document.getElementById('layerControls');controls.innerHTML='<div class="control-row"><strong>Prediction surface</strong></div><div class="control-row"><label><input type="radio" name="surface" checked> None</label></div>';const none=controls.querySelector('input');none.onchange=()=>showRaster(null);let group='';state.manifest.rasters.forEach(r=>{const b=[[r.bounds.south,r.bounds.west],[r.bounds.north,r.bounds.east]];rasters[r.id]={layer:L.imageOverlay(base+r.png,b,{pane:'soilRaster',opacity:.8}),r};if(r.group!==group){controls.insertAdjacentHTML('beforeend',`<div class="control-row"><small>${esc(r.group||'')}</small></div>`);group=r.group}controls.insertAdjacentHTML('beforeend',`<div class="control-row"><label><input type="radio" name="surface" value="${r.id}">${esc(r.title)}</label></div>`);controls.lastElementChild.querySelector('input').onchange=()=>showRaster(r.id)});controls.insertAdjacentHTML('beforeend','<div class="control-row"><label>Surface opacity <input id="opacity" type="range" min="0" max="1" step=".05" value=".8"></label></div>');document.getElementById('opacity').oninput=e=>{state.rasterOpacity=+e.target.value;if(state.activeRaster)rasters[state.activeRaster].layer.setOpacity(state.rasterOpacity)};addToggle('Mapped soil series','soil',soil);addToggle('SSURGO agreement','match',match);addToggle('SSURGO unit boundaries','boundary',units,true);addToggle('Pedon points','pedons',pedons,true);soil.remove();match.remove();fetch(base+'maplayers/soil_series.geojson').then(r=>r.json()).then(g=>{soil.addData(g);g.features.forEach(f=>{const k=f.properties?.series||'Unknown';state.seriesCounts[k]=(state.seriesCounts[k]||0)+1});renderLegend()});fetch(base+'maplayers/ssurgo_matchup.geojson').then(r=>r.json()).then(g=>{match.addData(g);g.features.forEach(f=>{const k=f.properties?.match||'Unknown';state.matchCounts[k]=(state.matchCounts[k]||0)+1});renderLegend()});fetch(base+'maplayers/ssurgo_units.geojson').then(r=>r.json()).then(g=>units.addData(g));fetch(base+'maplayers/site_border.geojson').then(r=>r.json()).then(g=>{border.addData(g);try{map.fitBounds(border.getBounds(),{padding:[24,24]})}catch(e){}});Papa.parse(base+'data/perry_FP_samples_80.csv',{download:true,header:true,complete:r=>addMarkers(r.data)});renderLegend()}
function addToggle(label,key,layer,checked=false){const row=document.createElement('div');row.className='control-row';row.innerHTML=`<label><input type="checkbox" ${checked?'checked':''}> ${label}</label>`;row.querySelector('input').onchange=e=>{state[key]=e.target.checked;if(e.target.checked)layer.addTo(map);else layer.remove();renderLegend()};document.getElementById('layerControls').appendChild(row)}function showRaster(id){Object.values(rasters).forEach(x=>x.layer.remove());state.activeRaster=id;if(id){rasters[id].layer.setOpacity(state.rasterOpacity);rasters[id].layer.addTo(map)}renderLegend()}loadMap();
</script>
