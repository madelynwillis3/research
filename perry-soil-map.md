---
layout: bare
title: "Perry Soil Map"
permalink: /perry-soil-map/
---

<link rel="stylesheet" href="https://unpkg.com/leaflet/dist/leaflet.css">
<style>
#mapWrap{display:flex;gap:16px;align-items:flex-start;flex-wrap:wrap;margin-bottom:1.5em}
#legendColumn{flex:0 0 240px;max-width:240px;position:sticky;top:12px;align-self:flex-start;order:0}
#mapColumn{flex:1 1 480px;min-width:320px}
#map{height:760px;width:100%;border-radius:12px}
#infoPanel{flex:1 1 300px;min-width:280px;max-width:460px;padding:12px 14px;border:1px solid rgba(0,0,0,.15);border-radius:12px;background:#fff;position:sticky;top:12px}
#infoPanel h3{margin:0 0 6px}.muted{opacity:.8;font-size:.95rem}
#legend{padding:12px 14px;border:1px solid rgba(0,0,0,.15);border-radius:12px;background:#fff;margin:0;max-height:calc(100vh - 40px);overflow-y:auto;font-size:.9rem}
.legend-header{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:8px}.legend-header h4{margin:0}
#resetLegendBtn{display:none}.legend-section{padding:10px 0;border-top:1px solid rgba(0,0,0,.09)}.legend-section:first-child{border-top:0;padding-top:0}.legend-section h5{margin:0 0 6px;font-size:.82rem;text-transform:uppercase;letter-spacing:.04em;color:#444}.legend-section .sub{margin:0 0 6px;font-size:.75rem;color:#666}.legend-grid{display:grid;grid-template-columns:1fr;gap:2px}.legend-item{display:flex;align-items:center;gap:8px;font-size:.85rem;padding:4px 6px;border-radius:6px;cursor:pointer;border:0;background:transparent;text-align:left;width:100%}.legend-item.static{cursor:default}.legend-item.static:hover{background:transparent}.legend-swatch{width:14px;height:14px;border:1.5px solid #000;flex:0 0 14px}.legend-swatch.dot{border-radius:50%}.legend-swatch.line{height:0;border:0;border-top:2.5px solid;width:20px;flex:0 0 20px}.ramp{display:flex;flex-direction:column;gap:1px}.ramp-row{display:flex;align-items:center;gap:8px;font-size:.78rem}.ramp-chip{width:20px;height:13px;border:1px solid rgba(0,0,0,.25)}.legend-note{margin:8px 0 0;font-size:.72rem;color:#666;line-height:1.35}.soil-raster img{image-rendering:pixelated}
#layerControls{margin-top:12px;padding:12px 14px;border:1px solid rgba(0,0,0,.15);border-radius:12px;background:#fff}#layerControls label{display:inline-block;margin:4px 14px 4px 0}#layerControls .control-heading{display:block;margin:8px 0 2px;font-weight:700}#layerControls button{border:1px solid rgba(0,0,0,.25);border-radius:7px;background:#f7f7f7;padding:6px 10px;cursor:pointer}#opacityControl{width:150px}.raster-legend{display:flex;height:22px}.raster-legend span{flex:1}
@media(max-width:900px){#legendColumn{flex:1 1 100%;max-width:none;position:static;order:3}#infoPanel{position:static}.legend-grid{grid-template-columns:repeat(auto-fit,minmax(170px,1fr))}}@media(max-width:700px){#map{height:620px}#layerControls label{display:block}}
</style>

<h1>Perry Soil Map</h1>
<p>This interactive map shows pedon observations at UGA GrandFarm in Perry, Georgia, together with ordinary-kriged predictions of soil chemistry and texture. Use the layer controls to compare surfaces and mapped soil boundaries.</p>
<div id="mapWrap">
  <aside id="legendColumn"><div id="legend" aria-label="Map legend"><div class="legend-header"><h4 id="legendTitle">Legend</h4><button id="resetLegendBtn" type="button">Show all</button></div><div id="legendContent"></div><p id="unitNote" class="legend-note"></p></div></aside>
  <div id="mapColumn"><div id="map"></div></div>
  <div id="infoPanel" aria-live="polite"><h3>Welcome</h3><p class="muted">Click a pedon point to view its soil profile and field photo.</p></div>
</div>

<script>
(function(){
  var base='{{ "/assets/" | relative_url }}', legend, content, mapWrap, mapColumn, seriesSection, rasterSection, matchSection, boundarySection, pointsSection, manifest;
  function node(tag, cls, text){var n=document.createElement(tag);if(cls)n.className=cls;if(text!=null)n.textContent=text;return n;}
  function section(title){var s=node('section','legend-section'),h=node('h5',null,title);s.appendChild(h);return s;}
  function staticItem(color,label,kind){var d=node('div','legend-item static'),sw=node('span','legend-swatch '+(kind||''));if(kind==='line')sw.style.borderTopColor=color;else sw.style.background=color;d.append(sw,node('span',null,label));return d;}
  function fmt(v){return Math.abs(v)>=100?Number(v).toFixed(0):Math.abs(v)>=10?Number(v).toFixed(1):Number(v).toFixed(2);}
  function raster(r){var s=section(r.title+(r.unit?' ('+r.unit+')':'')),ramp=node('div','ramp');for(var i=r.colors.length-1;i>=0;i--){var row=node('div','ramp-row'),chip=node('span','ramp-chip');chip.style.background=r.colors[i];row.append(chip,node('span',null,fmt(r.breaks[i])+' – '+fmt(r.breaks[i+1])));ramp.append(row);}s.append(ramp);return s;}
  function moveGenerated(){if(!content)return;var kids=Array.prototype.slice.call(content.children);if(kids.some(function(x){return x.classList.contains('raster-legend');})){if(!rasterSection){rasterSection=section('Active surface');content.insertBefore(rasterSection,content.firstChild);}kids.forEach(function(x){if(x!==rasterSection&&x.classList.contains('raster-legend')||x.classList.contains('raster-labels'))rasterSection.append(x);});}else if(kids.some(function(x){return x.classList.contains('legend-item');})){if(!pointsSection){pointsSection=section('Pedon points (soil series)');content.appendChild(pointsSection);}kids.forEach(function(x){if(x.classList.contains('legend-item'))pointsSection.append(x);});}}
  function updateStatic(){if(!manifest||!document.getElementById('layerControls'))return;var cb=document.querySelectorAll('#layerControls input[type=checkbox]');var showUnits=!!document.querySelector('#layerControls input[data-layer="ssurgo_units"]:checked'),showBorder=!!document.querySelector('#layerControls input[data-layer="site_border"]:checked'),showSeries=!!document.querySelector('#layerControls input[data-layer="soil_series"]:checked'),showMatch=!!document.querySelector('#layerControls input[data-layer="ssurgo_matchup"]:checked');if(showSeries&&!seriesSection){seriesSection=section('Mapped soil series');var counts={};fetch(base+manifest.vectors.soil_series.file).then(function(r){return r.json();}).then(function(g){g.features.forEach(function(f){var k=f.properties.series;counts[k]=(counts[k]||0)+1;});Object.keys(counts).sort(function(a,b){return(a==='HTM')-(b==='HTM')||counts[b]-counts[a]||a.localeCompare(b);}).forEach(function(k){seriesSection.append(staticItem((manifest.vectors.soil_series.palette||{})[k]||'#BDBDBD',k+' ('+counts[k]+')','box'));});});content.appendChild(seriesSection);}if(!showSeries&&seriesSection){seriesSection.remove();seriesSection=null;}if(showMatch&&!matchSection){matchSection=section('Field series vs SSURGO');var counts={};fetch(base+manifest.vectors.ssurgo_matchup.file).then(function(r){return r.json();}).then(function(g){g.features.forEach(function(f){var k=f.properties.match;counts[k]=(counts[k]||0)+1;});matchSection.append(node('p','sub',(counts.Agree||0)+' of '+((counts.Agree||0)+(counts.Disagree||0))+' polygons agree'));['Agree','Disagree'].forEach(function(k){if(counts[k]!=null)matchSection.append(staticItem((manifest.vectors.ssurgo_matchup.palette||{})[k],k+' ('+counts[k]+')','box'));});});content.appendChild(matchSection);}if(!showMatch&&matchSection){matchSection.remove();matchSection=null;}if(showUnits||showBorder){if(!boundarySection){boundarySection=section('Boundaries');content.appendChild(boundarySection);}boundarySection.innerHTML='<h5>Boundaries</h5>';if(showUnits){var u=staticItem('#555','SSURGO map unit','line');u.querySelector('.legend-swatch').style.borderTopStyle='dashed';boundarySection.append(u);}if(showBorder){var b=staticItem('#000','Site border','line');boundarySection.append(b);}}else if(boundarySection){boundarySection.remove();boundarySection=null;}}
  function enhance(){legend=document.getElementById('legend');content=document.getElementById('legendContent');mapWrap=document.getElementById('mapWrap');mapColumn=document.getElementById('mapColumn');if(!legend||!content||!mapWrap||!mapColumn)return;if(!document.getElementById('layerControls'))return;if(legend.parentElement.id!=='legendColumn'){var col=node('aside');col.id='legendColumn';col.append(legend);mapWrap.insertBefore(col,mapColumn);}if(!manifest)fetch(base+'maplayers/manifest.json').then(function(r){return r.json();}).then(function(m){manifest=m;updateStatic();});moveGenerated();updateStatic();}
  var timer=setInterval(enhance,150);setTimeout(function(){clearInterval(timer);},30000);document.addEventListener('change',function(e){if(e.target.closest&&e.target.closest('#layerControls'))setTimeout(enhance,20);});
}());
</script>
